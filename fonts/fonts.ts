import { Roboto, Nunito } from 'next/font/google'

export const roboto = Roboto({ subsets: ['latin'], weight: ['900'], display: 'swap' })

export const nunito = Nunito({
	weight: '700',
	subsets: ['latin'],
	display: 'swap',
})
